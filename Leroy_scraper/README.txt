Prerequisites:
- Python 3.8
- requests (pip install requests)
- lxml (pip install lxml)

Entry function in LeroyScraper offers you an example of how to  use the scraper. LeroyScraper.scrap() is documented to help you get going. 